$('#check-card-form').html("<%= j render partial: 'home/form', locals: { random_card: @random_card } %>");
$('.flash-messages').html("<%= j render partial: 'layouts/flash', locals: { flash: flash } %>");
